Urban Health Intelligence System - Issue Tracker (Updated)

=== COMPLETED FIXES ===

1) [FIXED] Critical Routing / App Boot Issues
- `app/_layout.tsx` rewritten with Stack navigator + AuthProvider; invalid hook usage resolved.
- `app/index.tsx` created to redirect root path to /login.
- All unmatched routes now redirect correctly.

2) [FIXED] Authentication Backend
- `backend/migrations/015_users.sql` — users table created.
- `backend/seed/009_users_seed.sql` — seed users (patient/hospital/municipal) added.
- `backend/src/controllers/auth.controller.js` — register, login, updateProfile, resetPassword implemented.
- `backend/src/routes/auth.routes.js` — POST /login, /register, /reset-password, PATCH /profile.
- Auth routes registered in `backend/src/app.js`.

3) [FIXED] Authentication Frontend
- `context/AuthContext.tsx` — AuthProvider with login/logout/updateUser; user state available app-wide.
- `app/login.tsx` — calls /api/auth/login, stores user in context, redirects by role.
- `app/create-account/patient.tsx` — calls /api/auth/register (role: PATIENT).
- `app/create-account/hospital.tsx` — calls /api/auth/register (role: HOSPITAL).
- `app/create-account/municipal.tsx` — calls /api/auth/register (role: MUNICIPAL).
- `app/forgot-password.tsx` — calls /api/auth/reset-password with email + phone validation.
- Logout buttons in hospital, municipal, and profile screens now call logout() from AuthContext.

4) [FIXED] Role-Based Tabs
- `app/(tabs)/_layout.tsx` — shows only the tab relevant to the logged-in role; Profile tab added for patients.

5) [FIXED] Identity / Hardcoded Data
- `HOSPITAL_ID = 1` removed from hospital/appointment.tsx, capacity.tsx, reports.tsx, index.tsx — now sourced from user.hospital_id.
- Patient name "Priya Sharma" replaced with user.name from AuthContext throughout dashboard screens.
- Ward/insurance in report.tsx and find-hospital.tsx now sourced from authenticated user.

6) [FIXED] Patient Appointment Screen
- `app/(tabs)/dashboard/appointment.tsx` rewritten to fetch real appointments from /api/appointments filtered by patient_name; supports cancel; pull-to-refresh.

7) [FIXED] Patient Health Timeline
- `app/(tabs)/dashboard/health-timeline.tsx` now fetches real reports and appointments; replaces static mock timeline.

8) [FIXED] Patient Dashboard Risk Persistence
- `app/(tabs)/dashboard/index.tsx` fetches latest risk from /api/reports on mount; falls back to URL params if no DB data.

9) [FIXED] Find Hospital Booking
- `app/(tabs)/dashboard/find-hospital.tsx` now books using authenticated user name and insurance from context.

10) [FIXED] Report Screen
- `app/(tabs)/dashboard/report.tsx` — patient name and ward sourced from auth context; result state properly typed; router.push fixed.

11) [FIXED] Profile Persistence
- `app/(tabs)/profile/index.tsx` — loads fields from auth context; Save calls PATCH /api/auth/profile with user.id; updates context on success.

12) [FIXED] Backend patient_name Filtering
- `appointment.controller.js` and `report.controller.js` support ?patient_name= query filter.

13) [FIXED] Municipal Outbreak Buttons
- "Investigate" navigates to /(tabs)/municipal/heatmap.
- "Send Alert" navigates to /(tabs)/municipal/advisory.
- Back button fixed to use /(tabs)/municipal route.

14) [FIXED] Python Spawn Cross-Platform
- `outbreak.controller.js` now uses `python3` on macOS/Linux, `py` on Windows.

=== REMAINING / KNOWN GAPS (OPEN) ===

1) Auth persistence across app restarts
- User session is in-memory React state only; closing the app logs the user out.
- Fix: persist user to AsyncStorage/SecureStore on login and rehydrate in AuthContext on mount.

2) Real-time sync (polling / websockets)
- Screens refresh on mount or manual pull-to-refresh only; no push/polling mechanism.
- Fix: add interval polling (e.g. every 30s) or WebSocket for hospital appointment/capacity screens.

3) Password security
- Passwords stored and compared as plain text in DB.
- Fix: add bcrypt hashing in register and compare in login.

4) JWT / session tokens
- No token-based auth; any client can call any API endpoint.
- Fix: issue JWT on login, verify via middleware on protected routes.

5) Find-hospital booking UX
- Appointment time is still hardcoded to "10:30 AM" in patient booking flow.
- Fix: add date/time picker before calling POST /api/appointments.

6) Hospital recommendation → DB mapping quality
- CSV hospital names do not exactly match DB hospital names.
- Current fallback maps recommendations to available DB hospital IDs (bookable, but approximate).
- Fix: add a canonical mapping table (csv_hospital_name -> hospitals.id) for accurate booking destination.

7) Automated test coverage
- No end-to-end tests exist for login, register, report, appointment, or cross-role sync flows.
- Fix: add Jest/Detox tests for critical paths.

=== LATEST SMOKE TEST (2026-04-23) ===

- API health: PASS (`GET /`)
- Patient login: PASS (`POST /api/auth/login`)
- Hospital recommendations: PASS (`GET /api/hospitals/recommend`)
- Appointment creation: PASS (`POST /api/appointments`)
- Appointment cancellation: PASS (`PATCH /api/appointments/:id/status` -> `CANCELLED`)